{
  "name": "dep-deployment",
  "version": "1.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "clean": "rm -rf dist",
    "build": "npm run clean && parcel build --dist-dir docs --public-url /Parcel/"
  },
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}
